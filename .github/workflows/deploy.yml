name: Build & Deploy Docs

on:
  workflow_dispatch:
    inputs:
      version:
        description: "TunGuard release version"
        required: true
        type: string

  repository_dispatch:
    types:
      - release

permissions:
  contents: write

jobs:
  build:
    name: Build Documentation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout docs
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Get release version
        id: release
        env:
          EVENT_VERSION: ${{ github.event.client_payload.version }}
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          if [ -n "$EVENT_VERSION" ]; then
            VERSION="$EVENT_VERSION"
          else
            VERSION="$INPUT_VERSION"
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Setup Git User
        run: |
          git config --global user.name "Frost-bit-star"
          git config --global user.email "morganmilstone983@gmail.com"

      - name: Sync gh-pages
        run: |
          if git ls-remote --exit-code origin refs/heads/gh-pages >/dev/null 2>&1; then
            git fetch origin gh-pages
            git branch -f gh-pages origin/gh-pages
          fi

      - name: Deploy Documentation with Mike
        env:
          VERSION: ${{ steps.release.outputs.version }}
        run: |
          if [[ "$VERSION" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Stable release detected: $VERSION"

            MINOR_VERSION=$(echo "$VERSION" | cut -d. -f1,2)

            mike deploy \
              --push \
              --update-aliases \
              "$MINOR_VERSION" latest
          else
            echo "Pre-release detected: $VERSION"

            mike deploy \
              --push \
              "$VERSION"
          fi

      - name: Checkout generated site
        run: |
          git fetch origin gh-pages
          git checkout -B pages-site origin/gh-pages

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: .

  deploy:
    name: Deploy GitHub Pages
    runs-on: ubuntu-latest

    needs: build

    permissions:
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
